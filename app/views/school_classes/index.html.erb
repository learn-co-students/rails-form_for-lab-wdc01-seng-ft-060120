<h1>All School Classes</h1>

<div>
    <ul> 
        <% @school_classes.each do |school_class| %>
            <li> <%= link_to school_class.title, school_class_path(school_class.id) %> </li>
        <% end %> 
    </ul>
        <%= link_to "Add new School Class", new_school_class_path %> 
</div>