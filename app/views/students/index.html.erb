<h1>All Students</h1>

<div>
    <ul>
        <% @students.each do |student| %>
            <li><%= link_to student.to_s, student_path(student.id) %></li>
        <% end %>
    </ul>

        <%= link_to "Add New Student", new_student_path %>
</div>
